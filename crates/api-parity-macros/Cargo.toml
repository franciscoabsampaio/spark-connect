[package]
name = "api-parity-macros"
version = "0.1.0"
edition = "2024"
license = "Apache-2.0"
description = "Attribute macros for the api-parity tracker."

[lib]
proc-macro = true

[dependencies]
syn = { version = "2", features = ["full"] }
quote = "1"
proc-macro2 = "1"
