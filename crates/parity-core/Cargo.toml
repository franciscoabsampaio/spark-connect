[package]
name = "parity-core"
version = "0.1.0"
edition = "2024"
license = "Apache-2.0"
description = "Parity tracking primitives for spark-connect."

[dependencies]
inventory = "0.3"
parity-macros = { path = "../parity-macros" }