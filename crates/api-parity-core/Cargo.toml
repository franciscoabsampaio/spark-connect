[package]
name = "api-parity-core"
version = "0.1.0"
edition = "2024"
license = "Apache-2.0"
description = "Parity tracking primitives for spark-connect."

[dependencies]
inventory = "0.3"
api-parity-macros = { path = "../api-parity-macros" }